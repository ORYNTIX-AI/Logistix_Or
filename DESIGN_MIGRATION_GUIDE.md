# Руководство по переносу изменений дизайна

## 📋 Структура соответствия файлов

### `frontend_design_template.html` → Исходные файлы

| Секция в HTML шаблоне | Соответствующий файл | Что переносить |
|------------------------|---------------------|----------------|
| `<style>` → Стили из `index.css` | `frontend/src/index.css` | Базовые стили, Tailwind импорты |
| `<style>` → Стили из `App.css` | `frontend/src/App.css` | Кастомные стили, анимации, медиа-запросы |
| `<header>` секция | `frontend/src/App.js` → Компонент `App` | JSX структура Header |
| Hero Section | `frontend/src/App.js` → Компонент `App` | JSX структура Hero |
| Search Form | `frontend/src/App.js` → Компонент `SearchForm` | JSX структура формы |
| Search Results | `frontend/src/App.js` → Компонент `SearchResults` | JSX структура карточек результатов |
| Features Section | `frontend/src/App.js` → Компонент `App` | JSX структура секции особенностей |
| Footer | `frontend/src/App.js` → Компонент `App` | JSX структура Footer |

---

## 🔍 Как найти изменения

### Вариант 1: Использовать Git (рекомендуется)

```bash
# 1. Сохраните оригинальную версию шаблона
git add frontend_design_template.html
git commit -m "Design template for designer"

# 2. После получения изменений от дизайнера
git diff frontend_design_template.html
# или
git diff HEAD frontend_design_template.html
```

### Вариант 2: Использовать VS Code Compare

1. Откройте `frontend_design_template.html`
2. Правой кнопкой → "Select for Compare"
3. Откройте оригинальную версию (если сохранили)
4. Правой кнопкой → "Compare with Selected"

### Вариант 3: Использовать онлайн-инструменты

- [Diffchecker](https://www.diffchecker.com/)
- [Text Compare](https://text-compare.com/)

---

## 📝 Пошаговая инструкция по переносу изменений

### Шаг 1: Анализ изменений в CSS

#### 1.1. Изменения в базовых стилях (`index.css`)

Ищите изменения в секции:
```html
<style>
    /* ============================================
       СТИЛИ ИЗ index.css
       ============================================ */
    body { ... }
    code { ... }
</style>
```

**Что делать:**
- Скопируйте измененные стили
- Вставьте в `frontend/src/index.css`
- Сохраните порядок: сначала Tailwind импорты, потом кастомные стили

#### 1.2. Изменения в кастомных стилях (`App.css`)

Ищите изменения в секции:
```html
<style>
    /* ============================================
       СТИЛИ ИЗ App.css
       ============================================ */
    .App { ... }
    .result-card { ... }
    /* и т.д. */
</style>
```

**Что делать:**
- Скопируйте измененные стили
- Вставьте в `frontend/src/App.css`
- Проверьте, что классы совпадают с используемыми в JSX

### Шаг 2: Анализ изменений в HTML структуре

#### 2.1. Header (Шапка)

**В шаблоне:**
```html
<div class="bg-white shadow-sm">
    <!-- Header content -->
</div>
```

**В App.js ищите:**
```javascript
// В компоненте App, секция Header
<div className="bg-white shadow-sm">
    {/* Header content */}
</div>
```

**Что делать:**
- Скопируйте HTML структуру
- Конвертируйте `class` → `className`
- Конвертируйте атрибуты HTML → JSX (например, `for` → `htmlFor`)
- Убедитесь, что все Tailwind классы сохранены

#### 2.2. Hero Section

**В шаблоне:**
```html
<div class="relative bg-cover bg-center bg-no-repeat h-96">
    <!-- Hero content -->
</div>
```

**В App.js ищите:**
```javascript
// В компоненте App, секция Hero Section
<div 
    className="relative bg-cover bg-center bg-no-repeat h-96"
    style={{...}}
>
    {/* Hero content */}
</div>
```

#### 2.3. Search Form

**В шаблоне:**
```html
<form class="space-y-6">
    <!-- Form fields -->
</form>
```

**В App.js ищите:**
```javascript
// В компоненте SearchForm
<form onSubmit={handleSubmit} className="space-y-6">
    {/* Form fields */}
</form>
```

**Важно:**
- Сохраните все обработчики событий (`onSubmit`, `onChange`, и т.д.)
- Не удаляйте логику React (`useState`, `useEffect`, и т.д.)
- Только обновляйте структуру и классы

#### 2.4. Search Results

**В шаблоне:**
```html
<div class="bg-white rounded-xl shadow-lg ...">
    <!-- Result card -->
</div>
```

**В App.js ищите:**
```javascript
// В компоненте SearchResults
<div className="bg-white rounded-xl shadow-lg ...">
    {/* Result card */}
</div>
```

#### 2.5. Footer

**В шаблоне:**
```html
<footer class="bg-gray-800 text-white py-12 mt-16">
    <!-- Footer content -->
</footer>
```

**В App.js ищите:**
```javascript
// В компоненте App, секция Footer
<footer className="bg-gray-800 text-white py-12 mt-16">
    {/* Footer content */}
</footer>
```

---

## 🔄 Конвертация HTML → JSX

### Основные правила:

1. **Атрибуты классов:**
   - `class` → `className`
   - `for` → `htmlFor`

2. **Стили:**
   - `style="background-image: ..."` → `style={{backgroundImage: '...'}}`
   - Кавычки в JSX: одинарные для строк, двойные для атрибутов

3. **Комментарии:**
   - `<!-- комментарий -->` → `{/* комментарий */}`

4. **Самозакрывающиеся теги:**
   - `<input>` → `<input />`
   - `<img>` → `<img />`

5. **События:**
   - В HTML могут быть пустые обработчики
   - В JSX должны быть функции: `onClick={() => {}}`

---

## ✅ Чеклист перед коммитом

- [ ] Все CSS стили перенесены в правильные файлы
- [ ] HTML структура конвертирована в JSX
- [ ] Все `class` заменены на `className`
- [ ] Все обработчики событий сохранены
- [ ] Логика React не нарушена
- [ ] Tailwind классы сохранены
- [ ] Проверено в браузере (запущен `npm start`)
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в консоли терминала

---

## 🛠️ Полезные команды

```bash
# Запустить фронтенд для проверки
cd frontend
npm start

# Проверить синтаксис JSX
npm run build

# Проверить линтер
npm run lint
```

---

## 📌 Примеры переноса

### Пример 1: Изменение цвета кнопки

**В шаблоне дизайнера:**
```html
<button class="bg-red-500 hover:bg-red-600">
    Найти
</button>
```

**В App.js:**
```javascript
<button className="bg-red-500 hover:bg-red-600">
    Найти
</button>
```

### Пример 2: Добавление нового CSS класса

**В шаблоне дизайнера:**
```html
<style>
    .new-button {
        background: linear-gradient(45deg, #ff6b6b, #ee5a6f);
        border-radius: 20px;
    }
</style>
```

**В App.css:**
```css
.new-button {
    background: linear-gradient(45deg, #ff6b6b, #ee5a6f);
    border-radius: 20px;
}
```

**В App.js:**
```javascript
<button className="new-button">
    Найти
</button>
```

---

## 🆘 Если что-то пошло не так

1. **Откатите изменения:**
   ```bash
   git checkout frontend/src/App.js
   git checkout frontend/src/App.css
   ```

2. **Проверьте синтаксис JSX:**
   - Убедитесь, что все теги закрыты
   - Проверьте кавычки в атрибутах
   - Проверьте фигурные скобки в JSX

3. **Проверьте импорты:**
   - Убедитесь, что все компоненты импортированы
   - Проверьте пути к CSS файлам

4. **Очистите кэш:**
   ```bash
   cd frontend
   rm -rf node_modules/.cache
   npm start
   ```

---

## 📞 Контакты для вопросов

Если возникли проблемы с переносом изменений, проверьте:
1. Этот документ
2. Оригинальный код в `frontend/src/App.js`
3. Консоль браузера на наличие ошибок
