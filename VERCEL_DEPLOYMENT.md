# 🚀 Инструкции по деплою на Vercel

## ✅ Что исправлено

1. **Создан `vercel.json`** - правильная конфигурация для монорепозитория
2. **Создан корневой `package.json`** - для управления workspace
3. **Создан `api/index.py`** - для работы FastAPI на Vercel
4. **Обновлен `requirements.txt`** - в корне проекта
5. **Настроены переменные окружения** - для production

## 🔧 Конфигурация проекта

### Структура файлов:
```
├── vercel.json              # Конфигурация Vercel
├── package.json             # Корневой package.json
├── requirements.txt         # Python зависимости
├── api/
│   └── index.py            # FastAPI handler для Vercel
├── frontend/               # React приложение
│   ├── package.json
│   ├── build/              # Собранное приложение
│   └── ...
└── backend/                # FastAPI сервер
    ├── server.py
    └── requirements.txt
```

## 🚀 Шаги деплоя

### 1. Подготовка переменных окружения

В настройках проекта Vercel добавьте:

**Environment Variables:**
- `DATABASE_URL` = `postgresql://postgres.kzqrdicmhwvvpepbicgy:axoN!merX$100@aws-1-eu-north-1.pooler.supabase.com:6543/postgres`
- `REACT_APP_BACKEND_URL` = `https://your-project-name.vercel.app` (замените на ваш URL)

### 2. Деплой через Vercel CLI

```bash
# Установка Vercel CLI
npm i -g vercel

# Логин в Vercel
vercel login

# Деплой
vercel --prod
```

### 3. Деплой через GitHub

1. Подключите репозиторий к Vercel
2. Vercel автоматически определит настройки из `vercel.json`
3. Добавьте переменные окружения в настройках проекта

## 🔍 Проверка деплоя

### Frontend:
- Откройте `https://your-project-name.vercel.app`
- Проверьте, что React приложение загружается

### Backend API:
- Откройте `https://your-project-name.vercel.app/api/`
- Должна открыться документация FastAPI

### Тестовые эндпоинты:
- `GET /api/containers` - список контейнеров
- `GET /api/routes` - список маршрутов
- `POST /api/auth/login` - авторизация

## 🐛 Решение проблем

### Ошибка "react-scripts: command not found"
✅ **Исправлено** - добавлены правильные команды сборки в `vercel.json`

### Ошибка "Module not found"
✅ **Исправлено** - создан корневой `package.json` с workspace

### Ошибка "DATABASE_URL not found"
✅ **Исправлено** - добавлена переменная окружения в `vercel.json`

## 📝 Важные замечания

1. **Backend и Frontend** деплоятся как один проект на Vercel
2. **API маршруты** доступны по `/api/*`
3. **Frontend** обслуживается статически
4. **Переменные окружения** настраиваются в панели Vercel

## 🔄 Обновление проекта

После изменений в коде:
```bash
git add .
git commit -m "Обновление проекта для Vercel"
git push origin main
```

Vercel автоматически пересоберет и задеплоит проект.

---

**🎉 Проект готов к деплою на Vercel!**
