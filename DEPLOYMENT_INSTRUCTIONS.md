# CargoSearch - Инструкции для программиста по деплою

## 📋 Обзор проекта

Данный проект представляет собой платформу для поиска железнодорожных контейнерных перевозок между Китаем и странами СНГ, аналог Aviasales для cargo. В проект добавлен функционал **бронирования с системой торгов**.

### Новый функционал:
- ✅ **Модальное окно бронирования** с полной формой данных клиента
- ✅ **Анимированный процесс торгов** (5 этапов)
- ✅ **Backend API для бронирования** с отправкой webhook в n8n
- ✅ **Валидация данных** и информирование о сборе данных
- ✅ **Интеграция с системой уведомлений**

---

## 🛠 Технологический стек

**Frontend:**
- React 18+ с хуками
- Tailwind CSS для стилизации  
- Axios для API вызовов
- Адаптивный дизайн (mobile-first)

**Backend:**
- FastAPI (Python 3.11+)
- PostgreSQL с asyncpg
- JWT авторизация
- Pydantic для валидации
- aiohttp для webhook вызовов

---

## 🔧 Настройка переменных окружения

### Backend (.env файл в папке backend/):

```bash
# === КРИТИЧЕСКИ ВАЖНО: Настроить правильную строку подключения к PostgreSQL ===
DATABASE_URL=postgresql://username:password@host:port/database_name

# Примеры:
# Для Supabase: postgresql://postgres.[project]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# Для локальной PG: postgresql://postgres:password@localhost:5432/cargosearch
# Для Railway: postgresql://postgres:[password]@[host].railway.app:5432/railway
# Для Heroku: postgres://[heroku-generated-url]
```

### Frontend (.env файл в папке frontend/):

```bash
# URL бэкенда для API вызовов
REACT_APP_BACKEND_URL=http://localhost:8001

# Для продакшена замените на реальный URL:
# REACT_APP_BACKEND_URL=https://your-backend-domain.com
```

---

## 📦 Установка и запуск

### 1. Установка зависимостей

```bash
# Backend
cd backend
pip install -r requirements.txt

# Frontend  
cd ../frontend
yarn install
```

### 2. Настройка базы данных

```sql
-- Создайте базу данных PostgreSQL
CREATE DATABASE cargosearch;

-- Примечание: Таблицы создаются автоматически при первом запуске backend
```

### 3. Запуск сервисов

```bash
# Backend (порт 8001)
cd backend
uvicorn server:app --host 0.0.0.0 --port 8001 --reload

# Frontend (порт 3000)
cd frontend
yarn start
```

---

## 🎯 Новый функционал: Система бронирования

### 📝 Компоненты Frontend

**1. BookingModal компонент** (в App.js):
- Модальное окно с формой бронирования
- Валидация всех полей (email, телефоны, обязательные поля)
- Загрузка файлов
- Чекбокс для изменения условий поставки с выпадающим списком
- Информация о сборе данных

**2. BookingAnimation компонент** (в App.js):
- 5-этапная анимация процесса торгов:
  1. Отправка уведомлений перевозчикам
  2. Получение предложений  
  3. Выбор лучшего предложения
  4. Уведомление победителя
  5. Отправка данных клиенту

**3. Состояния для управления**:
```javascript
const [showBookingModal, setShowBookingModal] = useState(false);
const [selectedRoute, setSelectedRoute] = useState(null);
const [bookingData, setBookingData] = useState({...});
const [showBookingAnimation, setShowBookingAnimation] = useState(false);
```

### 🔗 Backend API Эндпоинты

**1. GET /api/delivery-terms**
```javascript
// Возвращает список условий поставки (Инкотермс)
// Используется для выпадающего списка в форме
```

**2. POST /api/booking** (требует авторизации)
```javascript
// Создает заявку на бронирование
// Отправляет webhook в n8n для запуска процесса торгов
// Сохраняет данные в таблицу bookings
```

### 📊 Структура данных бронирования

```javascript
const bookingData = {
  // Компания и контакты
  company_name: '',         // Название компании плательщика *
  contact_name: '',         // ФИ контакта загрузке *
  contact_phone: '',        // Телефон контакта *
  sender_phone: '',         // Телефон отправителя *
  factory_address: '',      // Адрес фабрики *
  confirmation_email: '',   // Email для подтверждения *
  
  // Условия поставки
  change_delivery_terms: false,  // Чекбокс изменение условия поставки
  delivery_terms: '',           // Условия из выпадающего списка
  
  // Товарные данные  
  tnved_code: '',              // Код ТНВЭД *
  delivery_conditions: '',     // Условия поставки *
  uploaded_files: [],          // Загруженные файлы
  
  // Техническая информация
  route_id: '',               // ID выбранного маршрута
  search_query: {}            // Оригинальный поисковый запрос
}
```

---

## 🔄 Интеграция с n8n (система торгов)

### Webhook данные отправляемые в n8n:

```javascript
{
  "booking_id": "uuid",
  "user_id": "uuid", 
  "user_email": "client@example.com",
  "company_name": "ООО Компания",
  "contact_name": "Иванов И.И.",
  "contact_phone": "+7900123456", 
  "sender_phone": "+7900654321",
  "factory_address": "Адрес фабрики",
  "confirmation_email": "booking@example.com",
  "delivery_terms": "EXW",
  "tnved_code": "1234567890",
  "delivery_conditions": "Условия поставки",
  "route_id": "uuid",
  "search_query": {...},
  "timestamp": "2025-01-08T10:00:00.000Z",
  "event_type": "booking_created"
}
```

### 🎯 Что должно происходить в n8n:

1. **Уведомление перевозчиков** (WA/WhatsApp)
2. **Сбор предложений** от перевозчиков
3. **Выбор лучшего предложения** 
4. **Уведомление победителя** с данными клиента
5. **Уведомление клиенту** с контактами перевозчика

---

## 🗄️ База данных

### Таблица `bookings` (создается автоматически):

```sql
CREATE TABLE IF NOT EXISTS bookings (
    id VARCHAR PRIMARY KEY,
    user_id VARCHAR NOT NULL,
    company_name VARCHAR NOT NULL,
    contact_name VARCHAR NOT NULL,
    contact_phone VARCHAR NOT NULL,
    sender_phone VARCHAR NOT NULL,
    factory_address TEXT NOT NULL,
    confirmation_email VARCHAR NOT NULL,
    change_delivery_terms BOOLEAN DEFAULT FALSE,
    delivery_terms VARCHAR,
    tnved_code VARCHAR NOT NULL,
    delivery_conditions VARCHAR NOT NULL,
    uploaded_files TEXT[], 
    route_id VARCHAR NOT NULL,
    search_query JSONB NOT NULL,
    status VARCHAR DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

---

## ⚙️ Настройка Webhook в админке

1. Войти в админку (логин: `admin`, пароль: `admin123`)
2. Настроить URL для webhook в разделе "Настройки Webhook"  
3. URL должен указывать на n8n workflow для обработки торгов

Пример URL:
```
https://your-n8n-instance.com/webhook/cargo-booking
```

---

## 🚀 Деплой

### Настройка продакшена:

**1. Backend деплой:**
- Настроить продакшен PostgreSQL  
- Обновить DATABASE_URL в .env
- Настроить CORS для frontend домена
- Деплой на Heroku/Railway/DigitalOcean

**2. Frontend деплой:**
- Обновить REACT_APP_BACKEND_URL на продакшен URL
- Билд: `yarn build`
- Деплой на Vercel/Netlify/Cloudflare Pages

**3. n8n настройка:**
- Создать workflow для обработки booking webhook
- Настроить интеграции с WhatsApp/Email
- Обновить webhook URL в админке

---

## 🐛 Отладка

### Проверка работы API:

```bash
# Проверка здоровья API
curl http://localhost:8001/api/

# Проверка условий поставки  
curl http://localhost:8001/api/delivery-terms

# Проверка webhook настроек (требует авторизации админа)
curl -H "Authorization: Bearer <admin_token>" http://localhost:8001/api/admin/webhook
```

### Логи:

```bash
# Backend логи (если используется supervisor)
tail -f /var/log/supervisor/backend.out.log
tail -f /var/log/supervisor/backend.err.log

# Или через uvicorn
uvicorn server:app --host 0.0.0.0 --port 8001 --log-level debug
```

---

## 📞 Контакты и поддержка

При возникновении вопросов:
1. Проверить переменные окружения
2. Убедиться в корректности строки подключения к БД
3. Проверить логи backend и frontend
4. Убедиться что webhook URL настроен и доступен

**Успешного деплоя! 🚀**