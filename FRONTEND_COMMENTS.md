# Frontend - Комментарии для программиста

## 📝 Изменения в App.js

### 1. Новые состояния для модального окна бронирования (строки ~1263-1276):

```javascript
// Состояния для управления модальным окном бронирования
const [showBookingModal, setShowBookingModal] = useState(false);      // Показать/скрыть модальное окно
const [selectedRoute, setSelectedRoute] = useState(null);             // Выбранный маршрут для бронирования
const [bookingData, setBookingData] = useState({                      // Данные формы бронирования
  company_name: '',                    // Название компании плательщика (обязательное)
  contact_name: '',                    // ФИ контакта загрузке (обязательное)
  contact_phone: '',                   // Телефон контакта (обязательное, с валидацией)
  sender_phone: '',                    // Телефон отправителя (обязательное, с валидацией) 
  factory_address: '',                 // Адрес фабрики (обязательное)
  confirmation_email: userEmail || '', // Email для подтверждения (обязательное, с валидацией)
  change_delivery_terms: false,        // Чекбокс "изменение условия поставки"
  delivery_terms: '',                  // Выбранные условия поставки (из выпадающего списка)
  tnved_code: '',                     // Код ТНВЭД (обязательное)
  delivery_conditions: '',            // Условия поставки (обязательное)
  uploaded_files: []                  // Массив загруженных файлов
});

// Состояния для анимации процесса торгов
const [showBookingAnimation, setShowBookingAnimation] = useState(false);
const [isSubmittingBooking, setIsSubmittingBooking] = useState(false);
const [bookingAnimationStep, setBookingAnimationStep] = useState(0);
```

### 2. Функция handleBooking (заменила handleRequestCalculation):

```javascript
// ВАЖНО: Эта функция вызывается при нажатии кнопки "📞 Забронировать" в результатах поиска
const handleBooking = (result) => {
  // Проверка авторизации пользователя
  if (!userEmail) {
    alert("Пожалуйста, авторизуйтесь или зарегистрируйтесь, чтобы забронировать.");
    return;
  }
  
  // Сохранение выбранного маршрута и открытие модального окна
  setSelectedRoute(result);
  setBookingData(prev => ({
    ...prev,
    confirmation_email: userEmail  // Автозаполнение email текущего пользователя
  }));
  setShowBookingModal(true);
};
```

### 3. Функция handleBookingSubmit (отправка данных бронирования):

```javascript
// КРИТИЧЕСКИ ВАЖНО: Эта функция отправляет данные бронирования на backend
const handleBookingSubmit = async () => {
  setIsSubmittingBooking(true);
  
  try {
    // Подготовка данных для отправки на /api/booking
    const bookingRequest = {
      ...bookingData,                    // Все данные из формы
      route_id: selectedRoute.id,       // ID выбранного маршрута
      search_query: {                   // Контекст поиска
        origin_port: selectedRoute.origin_port,
        destination_port: selectedRoute.destination_port,
        carrier: selectedRoute.carrier,
        price_from_usd: selectedRoute.price_from_usd
      }
    };

    // Отправка POST запроса с JWT токеном
    const response = await axios.post(`${API}/booking`, bookingRequest, {
      headers: {
        'Authorization': `Bearer ${userToken}`,   // JWT токен для авторизации
        'Content-Type': 'application/json'
      }
    });

    // Закрытие модального окна и запуск анимации торгов
    setShowBookingModal(false);
    setShowBookingAnimation(true);
    setBookingAnimationStep(0);
    
    // ВАЖНО: Анимация показывает 5 этапов торгов с интервалом 2 секунды
    const animationSteps = [
      "Отправляем уведомления перевозчикам...",      // Этап 1: WA уведомления
      "Получаем предложения от перевозчиков...",     // Этап 2: Сбор предложений
      "Выбираем лучшее предложение...",              // Этап 3: Выбор победителя
      "Уведомляем победителя торгов...",             // Этап 4: Уведомление перевозчика
      "Отправляем данные клиенту..."                 // Этап 5: Уведомление клиента
    ];
    
    // Поэтапная анимация (каждый этап через 2 секунды)
    for (let i = 0; i < animationSteps.length; i++) {
      setTimeout(() => {
        setBookingAnimationStep(i);
      }, i * 2000);
    }
    
    // Финальное сообщение после завершения анимации
    setTimeout(() => {
      setShowBookingAnimation(false);
      setPopupMessage(`✅ Заявка успешно отправлена! Торги стартовали. Ожидайте результаты на почту ${bookingData.confirmation_email}`);
      setShowPopup(true);
      
      // Сброс формы после успешной отправки
      setBookingData({...}); // см. полный объект выше
    }, animationSteps.length * 2000 + 1000);

  } catch (error) {
    console.error('Booking submission error:', error);
    alert(`❌ Ошибка при отправке заявки: ${error.response?.data?.detail || error.message}`);
  } finally {
    setIsSubmittingBooking(false);
  }
};
```

---

## 🎨 Компонент BookingModal

### Ключевые особенности:

1. **Валидация полей** (функция `validateForm()`):
   - Email: регулярное выражение `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
   - Телефоны: регулярное выражение `/^\+?[\d\s\-\(\)]{10,}$/`
   - Обязательные поля: проверка на пустые значения

2. **Загрузка файлов** (функция `handleFileUpload()`):
   - Множественный выбор файлов
   - Сохранение имен файлов в `uploaded_files` массив
   - Возможность удаления загруженных файлов

3. **Условные поля**:
   - Выпадающий список условий поставки отображается только при активном чекбоксе
   - Данные загружаются с `/api/delivery-terms`

4. **Адаптивность**:
   - Responsive дизайн (grid для мобильных/десктоп)
   - Максимальная высота с прокруткой `max-h-[90vh] overflow-y-auto`

---

## 🎬 Компонент BookingAnimation

### Структура анимации:

```javascript
// 5 этапов с визуальными индикаторами
const steps = [
  { title: "...", description: "...", icon: "📱" },  // Этап 1
  { title: "...", description: "...", icon: "📋" },  // Этап 2  
  { title: "...", description: "...", icon: "🎯" },  // Этап 3
  { title: "...", description: "...", icon: "🏆" },  // Этап 4
  { title: "...", description: "...", icon: "📧" }   // Этап 5
];
```

### Визуальные состояния:
- **Активный этап**: синий фон, анимация pulse, спиннер
- **Завершенный этап**: зеленый фон, галочка ✓
- **Предстоящий этап**: серый фон, обычное состояние

---

## 🔗 Изменения в SearchResults компоненте

### Замена кнопки:
```javascript
// БЫЛО:
<button onClick={() => onRequestCalculation(result)}>
  📞 Запросить расчет
</button>

// СТАЛО:
<button onClick={() => onBooking(result)}>
  📞 Забронировать  
</button>
```

### Обновление пропсов:
```javascript
// БЫЛО: 
const SearchResults = ({ results, loading, onRequestCalculation }) => {

// СТАЛО:
const SearchResults = ({ results, loading, onBooking }) => {
```

---

## 📱 Рендеринг в основном компоненте

### Добавлены новые компоненты в JSX:

```javascript
{/* Модальное окно бронирования */}
{showBookingModal && (
  <BookingModal
    isOpen={showBookingModal}
    onClose={() => setShowBookingModal(false)}
    selectedRoute={selectedRoute}
    bookingData={bookingData}
    setBookingData={setBookingData}
    onSubmit={handleBookingSubmit}
    isSubmitting={isSubmittingBooking}
  />
)}

{/* Анимация процесса торгов */}
<BookingAnimation 
  isVisible={showBookingAnimation} 
  currentStep={bookingAnimationStep} 
/>
```

---

## 🚨 Важные моменты для программиста:

### 1. **Авторизация**:
- Модальное окно доступно только авторизованным пользователям
- JWT токен передается в заголовке `Authorization: Bearer ${userToken}`

### 2. **API интеграция**:
- `/api/delivery-terms` - GET запрос для загрузки условий поставки
- `/api/booking` - POST запрос для создания бронирования (требует авторизации)

### 3. **Состояние формы**:
- `confirmation_email` автоматически заполняется текущим email пользователя
- Форма сбрасывается после успешной отправки

### 4. **UX/UI**:
- Модальное окно блокирует взаимодействие во время отправки (`isSubmitting`)
- Анимация торгов показывает пользователю что происходит "за кулисами"
- Информационное уведомление о сборе данных для соответствия GDPR

### 5. **Обработка ошибок**:
- Показ alert с детализированным сообщением об ошибке
- Логирование ошибок в консоль для отладки

---

**Готово к интеграции с n8n для полного процесса торгов! 🚀**